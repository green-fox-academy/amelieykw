<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Cocktails</title>
</head>

<body>
    <h1>Cocktails</h1>
    <div><%= alcoholList %></div>
    <ul>
        <% for (let i = 0; i < cocktails.length; i++) { %>
        <% if (cocktails[i].contains.includes(alcoholType)) { %>
        <li>
            <%= cocktails[i].name %>
        </li>
        <% } %>
        <% } %>
    </ul>
</body>

</html>